{
  "name": "foster",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "TEST_WATCH=1 meteor test --driver-package meteortesting:mocha",
    "deploy-dev": "meteor-now --nosplit --now-options='--token $NOW_TOKEN'",
    "deploy-prod": "meteor-now --nosplit --now-options='--token $NOW_TOKEN' -e MONGO_URL=$MONGO_URL -e ROOT_URL=https://${DOMAIN} -e NODE_ENV=production"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0-beta.55",
    "meteor-node-stubs": "^0.4.1",
    "moment": "^2.22.2",
    "toastr": "^2.1.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.19.1",
    "eslint-config-airbnb": "^17.1.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-jsx-a11y": "^6.1.1",
    "eslint-plugin-react": "^7.11.1",
    "meteor-now": "^0.6.2",
    "now": "^11.3.10"
  }
}
